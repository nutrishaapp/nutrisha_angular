import { VideoLinkPipe } from './video-link.pipe';

describe('VideoLinkPipe', () => {
  it('create an instance', () => {
    const pipe = new VideoLinkPipe();
    expect(pipe).toBeTruthy();
  });
});
